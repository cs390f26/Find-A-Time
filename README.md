# Find-A-Time
Users create a poll with a set of candidate time slots for a meeting or event. Other users then mark which of those time slots work for them (available/not available). The system aggregates the responses and displays a count of how many people are available for each slot, helping the group identify the best time to meet.
